---
name: Aperture points
description: Conceptual frame for what KTIs filter for — absorb into KTI docs once we see usage
type: feature
---

The notebook describes "aperture points" as the filter through which a KTI focuses. Currently captured implicitly in `key_trend_indicators.threshold_definition`. Revisit if usage patterns suggest a structured field.
